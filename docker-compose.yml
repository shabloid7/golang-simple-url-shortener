services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - redis
    environment:
      - REDIS_ADDR=redis:6379
      - SERVER_PORT=8080
      - BASE_URL=http://localhost:8080

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
